@model WebApplication1.Models.Settings
@{
    Layout = "~/Views/Shared/_LayoutPageLoggedIn.cshtml";
    ViewBag.Title = "ChangePassword";
}

@using (Html.BeginForm("ChangePassword", "Patient", FormMethod.Post, new { @class = "form-horizontal" }))
{
    <fieldset>
        <legend>Ändra inställning</legend>
        <div class="form-group">
            <div class="col-md-4">
                @Html.HiddenFor(a => a.id, new { @Value = ViewBag.userid })
            </div>
        </div>
        <div class="form-group">
            <label class="col-md-4 control-label" for="piNewPass">Nytt lösenord</label>
            <div class="col-md-4">
                @Html.TextBoxFor(a => a.value, null, new { @class = "form-control input-md", placeholder = "Nytt lösenord" })
            </div>
        </div>
        <div class="form-group">
            <label class="col-md-4 control-label" for="bCancel"></label>
            <div class="col-md-8">
                @Html.ActionLink("Tillbaka", "Index", "Patient", new { @class = "btn btn-danger" })
                <button type="submit" id="bGodkend" name="bGodkend" class="btn btn-success">Spara</button>
            </div>
        </div>
    </fieldset>
}